@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Erro</h1>
<h2 class="text-danger">Ocorreu um erro ao processar sua requisição.</h2>

@if (!string.IsNullOrEmpty(Model.UserMessage))
{
    <div class="alert alert-danger">@Model.UserMessage</div>
}

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de Desenvolvimento</h3>
<p>
    Ative o ambiente <strong>Development</strong> para exibir informações detalhadas sobre o erro ocorrido.
</p>
<p>
    <strong>O ambiente Development não deve ser habilitado em produção.</strong>
    Isso pode resultar na exibição de informações sensíveis para o usuário final.
    Para depuração local, defina a variável de ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> como <strong>Development</strong>
    e reinicie o aplicativo.
</p>
